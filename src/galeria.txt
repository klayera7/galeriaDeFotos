# 📸 Galeria de Fotos - Pexels API

Projeto desenvolvido como parte de um desafio técnico Front-end. Trata-se de uma aplicação web responsiva que consome a API da Pexels para exibir fotos de alta qualidade, com funcionalidade de pesquisa dinâmica e interface em tema escuro (Dark Mode).

## 🚀 Funcionalidades

- **Busca Dinâmica:** Pesquise por qualquer termo (ex: "Tecnologia", "Natureza") e receba fotos atualizadas instantaneamente.
- **Consumo de API:** Integração assíncrona com a Pexels API utilizando `fetch` e `async/await`.
- **Interface Responsiva:** Layout que se adapta a desktops, tablets e smartphones (Grid Layout).
- **Feedback Visual:** Indicador de carregamento (Spinner) enquanto as requisições são processadas.
- **Tratamento de Erros:** Mensagens amigáveis caso a busca não retorne resultados ou a API falhe.
- **Semântica HTML:** Estrutura de código acessível e organizada.

## 🛠️ Tecnologias Utilizadas

- **HTML5:** Estruturação semântica (`<main>`, `<article>`, etc).
- **CSS3:**
  - Variáveis CSS (`:root`) para paleta de cores.
  - Flexbox e CSS Grid para layout.
  - Tema Escuro com tons de Roxo.
- **JavaScript (ES6+):**
  - ES Modules (`import`/`export`) para organização do código.
  - Manipulação do DOM.
  - Consumo de API REST.
- **Ferramentas Externas:**
  - [Pexels API](https://www.pexels.com/api/) (Fonte das imagens).
  - [Font Awesome](https://fontawesome.com/) (Ícones).

## 📂 Estrutura do Projeto

```text
/
├── index.html          # Estrutura principal
├── README.md           # Documentação
└── src/
    ├── CSS/
    │   └── style.css   # Estilização global
    └── JS/
        ├── script.js   # Ponto de entrada do JavaScript
        └── modules/
            └── APY_KEY.js # Lógica de conexão com a API